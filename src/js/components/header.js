const header = () => {
  const headerStr = `
    <h1>Learning parcel and babel</h1>
    <p>
      Parcel is module bundler<br>
      Babel is js transcompailer
    </p>
  `

  return headerStr
}

export default header
