{
	"name": "parcel",
	"version": "1.0.0",
	"description": "Parcel, Learning parcel module bundler",
	"browserslist": "> 0.1%, last 5 versions",
	"scripts": {
		"dev": "npm run clean && parcel src/index.html",
		"build": "npm run clean && parcel build src/*.html --public-url ./",
		"clean": "rimraf ./dist && rimraf ./.parcel-cache"
	},
	"keywords": [],
	"author": "James Alderson",
	"license": "MIT",
	"devDependencies": {
		"@parcel/transformer-sass": "^2.7.0",
		"parcel": "^2.7.0",
		"rimraf": "^3.0.2",
		"sharp": "^0.29.3"
	}
}
